name: svc1

on:
  repository_dispatch:
    types:
      - 'svc1'

jobs:
  report-status:
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
        if: ${{ inputs.success }}
      - run: exit 1
        if: ${{ ! inputs.success }}


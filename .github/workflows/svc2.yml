name: svc2

on:
  repository_dispatch:
    types:
      - 'svc2'

jobs:
  report-status:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ inputs }}
      - run: exit 0
        if: ${{ inputs.success }}
      - run: exit 1
        if: ${{ ! inputs.success }}


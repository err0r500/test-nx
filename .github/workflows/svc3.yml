name: svc3

on:
  repository_dispatch:
    types:
      - 'svc3'

jobs:
  report-status:
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
        if: ${{ github.event.client_payload.success }}
      - run: exit 1
        if: ${{ ! github.event.client_payload.success }}

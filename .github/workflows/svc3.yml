name: svc3

on:
  repository_dispatch:
    types:
      - 'svc3'

jobs:
  report-status:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ inputs }}
      - run: exit 0
        if: ${{ inputs.success }}
      - run: exit 1
        if: ${{ ! inputs.success }}

